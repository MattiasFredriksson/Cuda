#pragma once


/* Single threaded sort func. */
void oddEvenSortSingle(int* arr, unsigned int arr_len);

/* Verify arr is sorted. */
void verify(int* arr, unsigned int arr_len);

/* Print function. */
void printArr(int* arr, unsigned int from, unsigned int to);